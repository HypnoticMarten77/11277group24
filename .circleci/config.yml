version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/node:stable
      
jobs:
  build:
    environment:
      email: jondoe@organization.com
      password: pass123
    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          npm install

workflows:
  my-custom-workflow:
    jobs:
      - build