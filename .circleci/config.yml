version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/node:17.0.1
      
jobs:
  build:
    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          name: Install dependencies
          command: cd project-power-smart
          command: npm install
      - run: |
          name: Run tests
          command: npm run test

workflows:
  my-custom-workflow:
    jobs:
      - build